import React from "react";
import { Box, Button } from "@chakra-ui/react";

const Home = () => {
  return (
    <Box>
      <Button colorScheme={"blue"}>Keresztmetszet</Button>
      <Button colorScheme={"blue"}>Idősor</Button>
      <Button colorScheme={"blue"}>Panel</Button>
    </Box>
  );
};

export default Home;
